#include <iostream>
using namespace;

pair<int, int> minimax(..) {
	...
	  ...
	  return {-1, 1};
	...

	...
}

int main() {
	pair<int, int> p;
	p.first = 2;
	p.second = 4;

	vector< pair<int, int> > G;

	G[1].push_back(edge(2, 3));

	auto p = f();
	p.first.second


	return 0;
}