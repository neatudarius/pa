#include <cstdio>
#include <iostream>

using namespace std;

int main() {
	freopen("gigel.in",  "r", stdin);   // redirectez gigel.in la stdin
	freopen("gigel.out", "w", stdout);  // redirectez stdout in gigel.out

	int x, y;
	cin >> x >> y;
	x = 1; y = 2;
	cout << x << y;

	return 0;
}